import type { ReactNode } from "react";

/** Feature-layer shell for My Work Queue. Compose feature pages/components here. */
export function MyWorkQueueFeatureRoot({ children }: { children?: ReactNode }) {
	return <>{children}</>;
}
